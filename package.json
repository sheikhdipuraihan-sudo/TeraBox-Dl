{
  "name": "terabox-dl-api",
  "version": "2.0.0",
  "description": "TeraBox direct download metadata API for Vercel",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "vercel dev",
    "start": "vercel dev"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
